import { RecordItem, AuditLog, UserProfile } from '@/types';

// Demo accounts have been completely removed.
export const INITIAL_PROFILES: Record<string, UserProfile> = {};

// Clean slate: all pre-populated/mock records removed
export const INITIAL_RECORDS: RecordItem[] = [];

// Clean slate: all pre-populated audit logs removed
export const INITIAL_AUDIT_LOGS: AuditLog[] = [];
